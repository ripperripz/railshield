# Compose requires these two values. Generate different secrets; never commit .env.
POSTGRES_PASSWORD=replace-with-a-long-random-password
RAILSHIELD_API_KEY=replace-with-at-least-32-random-characters
RAILSHIELD_ENVIRONMENT=production
RAILSHIELD_CORS_ORIGINS=["http://localhost:8080"]
